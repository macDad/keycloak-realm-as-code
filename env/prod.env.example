# env/prod.env.example — copy to env/prod.env and fill in real values,
# or better: inject these at deploy time from Vault / AWS Secrets Manager /
# your platform's secret mechanism rather than a checked-out file.
# env/prod.env is gitignored; never commit it.
APP_BASE_URL=https://app.example.com
BACKEND_CLIENT_SECRET=
