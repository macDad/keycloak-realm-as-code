# env/dev.env — committed with throwaway local values only.
# Never put real secrets here; see env/staging.env.example and env/prod.env.example.
APP_BASE_URL=http://localhost:8080
BACKEND_CLIENT_SECRET=dev-throwaway-secret-do-not-use
